// var scrollContainer = document.querySelector('.outerproject');
// var scrollItems = document.querySelectorAll('.projects');

// var currentIndex = 0;
// var scrollInterval = 5000; // 5 seconds

// function scrollToNextItem() {
//   currentIndex++;
//   if (currentIndex >= scrollItems.length) {
//     currentIndex = 0;
//   }
//   scrollContainer.scrollTo({
//     left: scrollItems[currentIndex].offsetLeft,
//     behavior: 'smooth'
//   });
// }

// setInterval(scrollToNextItem, scrollInterval);
